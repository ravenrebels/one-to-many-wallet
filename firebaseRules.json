{
  "rules": {
    "ravencoinAddresses": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": false
      }
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        "$whatever": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "ravencoinAddresses": {
          ".read": "$uid === auth.uid",
          ".write": false
        }
      }
    },
    "transactions": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": false
      }
    },
    "assets": {
      ".read": true
    }
  }
}
